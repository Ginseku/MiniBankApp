spring:
  profiles:
    active: local
  datasource:
    url: jdbc:postgresql://localhost:5432/miniBank
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  jackson:
    serialization:
      indent_output: true
server:
  port: 8080